export type SelectorPizzaPropType = {
  types: number[];
  setActiveType: (activeType: number) => void;
  activeType: number;
  sizes: number[];
  setActiveSize: (activeSize: number) => void;
  activeSize: number;
};
